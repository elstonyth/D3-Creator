# See http://help.github.com/ignore-files/ for more about ignoring files.

# compiled output
dist
tmp
/out-tsc
.env
# dependencies
node_modules

# IDEs and editors
/.idea
.project
.classpath
.c9/
*.launch
.settings/
*.sublime-workspace
.vscode/*

# IDE - VSCode
.vscode/
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json

# misc
/.sass-cache
/connect.lock
/coverage
/libpeerconnection.log
npm-debug.log
yarn-error.log
testem.log
/typings

# Python bytecode (from .claude/skills scripts)
__pycache__/
*.pyc

# System Files
.DS_Store
Thumbs.db

# graphify knowledge-graph output (generated, large)
graphify-out/

.nx/cache
.nx/workspace-data

# Next.js
.next

# Vim files
**/*.swp
**/*.swo

# Temporary files
*.orig
*.~*~
*.tsbuildinfo

# ignore Secrets folder
.secrets/
libraries/plugins/src/plugins.ts
i18n.cache

# Generated by apps/frontend/scripts/fetch-gtm.mjs on install
apps/frontend/public/g.js

# Local install logs
install.log
.gstack/
__pycache__/

# D3 — local agent tooling, never commit
.claude/
.agents/

# D3 — exploration / scratch
design-system/
qa-report.md
skills-lock.json

# D3 — temporary local-only UI verification seed (delete after testing)
libraries/database/src/seed-ui-check.ts

# D3 — generated test reports (jest-junit writes reports/junit.xml on every `pnpm test`)
reports/
# D3 — legacy runtime artifacts (no longer used)
var/
dynamicconfig/
.vercel
.env*
!.env.local-ui.example

# Sentry Config File
.env.sentry-build-plugin

# Local tooling artifacts
.playwright-cli/
.playwright-mcp/
e2e-screenshots/
.vstate.txt

# D3 — one-off local verification/backfill scratch scripts (never commit)
.verify-scratch/

# D3 — local billing/receipt scripts (contain hardcoded API keys, personal data)
gen_summary.py
gen_cost_report.py
monthly_cost_report.py
fetch_receipts.py
outlook_token_setup.py
tikhub_invoice_download.py
tikhub_session_setup.py
download.html
*_D3-Cost-Summary.pdf
*_D3-Cost-Summary.txt

# D3 — internal product/planning docs. THE REPO IS PUBLIC (elstonyth/D3-Creator).
# plans/ holds PRD 1/2/3, amendment docs and pricing research. One `git add -A`
# without this rule publishes all of it permanently, into git history and
# GitHub's caches. Stage paths explicitly regardless; this is the backstop.
plans/

# D3 — the Script Coach playbook is the CLIENT'S PAID COURSE, condensed.
# THE REPO IS PUBLIC. Committing it publishes the product for free, forever.
# Production reads this text from Supabase at runtime, not from the bundle —
# see apps/frontend/src/content/README.md. The repo keeps no copy.
apps/frontend/src/content/d3-method.md
